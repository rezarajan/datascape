# flux-install - Flux controllers on the current kubeconfig context's
# cluster. Environment prerequisite (docs/plans/01-week-one.md), not
# compiled by d7s.
log "flux: install controllers"
flux install
