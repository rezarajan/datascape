# Doc consolidation log

**Class: Record (append-only).**

Additive-only doc growth with no consolidation cadence was a named platformctl defect
(`foundations/agentic-development.md`, known defects). Cadence: a human-authorized
consolidation pass at most **28 days** apart while docs are actively growing; the
SessionStart hook warns when the cadence lapses. Each pass appends a dated entry here:
what was consolidated, what was removed as duplication, what was left alone and why.

## 2026-07-25 — baseline

Repo established in consolidated form at solution setup: four foundation/discovery docs,
no known duplication. Nothing to consolidate; this entry starts the cadence clock.
